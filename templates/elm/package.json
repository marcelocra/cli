{
  "name": "PLACEHOLDER_PROJECT_NAME",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "license": "PLACEHOLDER_LICENSE",
  "scripts": {
    "__example::esbuild:node:dev": "esbuild ./main.ts --bundle --watch --sourcemap --format=esm --platform=node --outfile=main.js",
    "__example::esbuild:node:prod": "esbuild ./main.ts --bundle --minify --format=esm --platform=node --outfile=main.js",
    "__example::esbuild:browser": "# Remove `--platform=node` or change it to `browser` (default). For iife (default), remove `--format=esm` or change it to `iife`."
  },
  "dependencies": {},
  "devDependencies": {
    "@eslint/js": "9.10.0",
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "@types/node": "^20.14.5",
    "esbuild": "0.23.0",
    "eslint": "^9.8.0",
    "eslint-config-google": "^0.14.0",
    "eslint-config-prettier": "^9.1.0",
    "mermaid": "11.2.1",
    "npm-run-all2": "^6.2.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.3",
    "vite-elm-plugin": "tbd",
    "vite": "^5.3.5"
  },
  "engines": {
    "node": ">= 20"
  },
  "engineStrict": true,
  "author": {
    "name": "Marcelo Almeida",
    "email": "2532492+marcelocra@users.noreply.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/marcelocra/PLACEHOLDER_PROJECT_NAME"
  }
}
